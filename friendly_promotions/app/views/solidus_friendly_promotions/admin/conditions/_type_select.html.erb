<%= form_with(
    model: @condition || SolidusFriendlyPromotions::Condition.new(promotion: @promotion),
    scope: :condition,
    url: solidus_friendly_promotions.new_admin_promotion_promotion_action_condition_path(@promotion),
    method: :get
  ) do |form| %>
  <%= hidden_field_tag :level, @level %>
  <%= form.label :type %>
  <%= admin_hint SolidusFriendlyPromotions::Condition.human_attribute_name(:type), t(:promotions, scope: [:solidus_friendly_promotions, :hints, "spree/calculator"]) %>
  <%=
    form.select :type,
      options_for_condition_types(@promotion_action, @condition),
      {
        include_blank: t(:choose_condition, scope: 'solidus_friendly_promotions')
      },
      class: 'custom-select fullwidth',
      onchange: 'this.form.requestSubmit()',
      required: true
  %>
<% end %>
