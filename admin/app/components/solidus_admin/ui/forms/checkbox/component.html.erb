<label class="flex gap-2 items-center">
  <%= hidden_field_tag @field_name, "0" %>
  <%= render component("ui/checkbox").new(
    name: @field_name,
    value: "1",
    checked: @checked,
    **@attributes
  ) %>
  <%= label_text %>
  <%= hint if hint? %>
</label>
