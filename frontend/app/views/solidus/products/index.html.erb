<% content_for :sidebar do %>
  <div data-hook="homepage_sidebar_navigation">
    <% if "solidus/products" == params[:controller] && @taxon %>
      <%= render :partial => 'solidus/shared/filters' %>
    <% else %>
      <%= render :partial => 'solidus/shared/taxonomies' %>
    <% end %>
  </div>
<% end %>


<% if params[:keywords] %>

  <div data-hook="search_results">
    <% if @products.empty? %>
      <h6 class="search-results-title"><%= Solidus.t(:no_products_found) %></h6>
    <% else %>
      <%= render :partial => 'solidus/shared/products', :locals => { :products => @products, :taxon => @taxon } %>
    <% end %>
  </div>

<% else %>

  <div data-hook="homepage_products">
    <%= render :partial => 'solidus/shared/products', :locals => { :products => @products, :taxon => @taxon } %>
  </div>
<% end %>
