<ul class="admin-subnav" data-hook="admin_promotion_sub_tabs">
  <% if can? :admin, Solidus::Promotion %>
    <%= tab :promotions %>
  <% end %>
  <% if can? :admin, Solidus::PromotionCategory %>
    <%= tab :promotion_categories %>
  <% end %>
</ul>
